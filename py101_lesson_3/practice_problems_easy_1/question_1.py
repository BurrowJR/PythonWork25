# Will the code raise a error?

numbers = [1, 2, 3]
numbers[6] = 5  # IndexError: list assignment index out of range

# Yes, this code will raise an error because numbers[6] is out of range
# numbers only has 0, 1, 2 indexes